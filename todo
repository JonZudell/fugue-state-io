ui rewrite shadcn
  display-menu
    select / sort displays
    select associated catalan number
  
qol
  replace current media

playback
  eq

ideal implementation
  layout splits instead of playback area